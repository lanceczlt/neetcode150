a = [2, 1, 3, 5, 3, 2]

def sol(a): 
    track = set()


    for i in range(len(a)):
        
        if a[i] in track:
            return a[i]
        track.add(a[i])
        
    return -1

print(sol(a))